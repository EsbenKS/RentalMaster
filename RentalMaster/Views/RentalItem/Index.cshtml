@model IEnumerable<RentalMaster.Models.RentalItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RentalItemCategory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RentalItemMake)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RentalItemModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RentalItemCategory.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RentalItemMake.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RentalItemModel.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
